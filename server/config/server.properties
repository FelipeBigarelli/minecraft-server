# =============================================================
#  server.properties — Spigot 26.2
#  Copie para a raiz do servidor antes do primeiro boot.
# =============================================================

# --- Identidade ---------------------------------------------
motd=\u00A7bbiga.codes \u00A78| \u00A7fservidor de testes
server-port=25565
server-ip=
max-players=20

# --- Mundo --------------------------------------------------
level-name=world
level-seed=
level-type=minecraft\:normal
gamemode=survival
difficulty=normal
hardcore=false
pvp=true
allow-nether=true
generate-structures=true
spawn-monsters=true
spawn-protection=16

# --- Performance --------------------------------------------
# view-distance é o parâmetro que mais pesa em CPU e banda.
# 10 é o default; 6-8 é um bom equilíbrio em VPS modesta.
view-distance=8
# simulation-distance controla quantos chunks realmente "rodam"
# (mobs, redstone, crescimento). Baixar aqui alivia muito mais
# que baixar view-distance, e o jogador nem percebe.
simulation-distance=6
max-tick-time=60000
sync-chunk-writes=false
entity-broadcast-range-percentage=100

# --- Acesso / segurança -------------------------------------
# online-mode=true valida a conta na Mojang. NUNCA deixe false
# em servidor público: qualquer um entra com qualquer nick.
online-mode=true
white-list=false
enforce-whitelist=false
enforce-secure-profile=true
prevent-proxy-connections=false

# --- Anti-spam (novo na 26.2) -------------------------------
# A 26.2 separou o anti-spam de chat do de comandos.
# Valores em segundos; 10 é o default de ambos.
chat-spam-threshold-seconds=10
command-spam-threshold-seconds=10

# --- Administração ------------------------------------------
enable-command-block=false
op-permission-level=4
function-permission-level=2
broadcast-console-to-ops=true
broadcast-rcon-to-ops=true

# --- RCON (console remoto) ----------------------------------
# Deixe false a menos que precise. Se ativar, use senha forte e
# NUNCA exponha a porta 25575 na internet aberta.
enable-rcon=false
rcon.port=25575
rcon.password=

# --- Query --------------------------------------------------
enable-query=false
query.port=25565

# --- Misc ---------------------------------------------------
allow-flight=false
force-gamemode=false
player-idle-timeout=0
max-world-size=29999984
network-compression-threshold=256
require-resource-pack=false
resource-pack=
resource-pack-sha1=
hide-online-players=false
log-ips=true
